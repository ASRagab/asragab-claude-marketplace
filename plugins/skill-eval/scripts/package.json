{
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0"
  },
  "scripts": {
    "check": "bun build --no-bundle --target=bun *.ts --outdir /dev/null 2>&1 || true; bunx tsc --noEmit"
  }
}